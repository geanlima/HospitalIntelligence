import { CanActivateFn } from '@angular/router';

/**
 * Placeholder para autenticação futura (Fase 20).
 * Mantém a estrutura de guards pronta sem bloquear o acesso agora.
 */
export const authGuard: CanActivateFn = () => true;
